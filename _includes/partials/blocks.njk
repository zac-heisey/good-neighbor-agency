{% if block.type == 'richText' %}

  {# Rich Text block #}
  {{ macro.richText(block.body) }}

{% endif %}

{% if block.type == 'imageGrid' %}

  {# Image Grid block #}
  {{ macro.imageGrid(block.images) }}

{% endif %}
