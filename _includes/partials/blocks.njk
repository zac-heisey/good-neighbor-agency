{% if block.type == 'richText' %}

  {# Rich Text block #}
  {{ macro.richText(block.body) }}

{% endif %}

{% if block.type == 'imageGrid' %}

  {# Image Grid block #}
  {{ macro.imageGrid(block.images) }}

{% endif %}

{% if block.type == 'carousel' %}

  {# Carousel block #}
  {{ macro.carousel(block.items) }}

{% endif %}
